BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[users] ADD [isBlocked] BIT NOT NULL CONSTRAINT [users_isBlocked_df] DEFAULT 0,
[blockedReason] NVARCHAR(1000),
[blockedAt] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
