BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[products] ADD [garmentType] NVARCHAR(1000) NOT NULL CONSTRAINT [products_garmentType_df] DEFAULT 'TEE';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
