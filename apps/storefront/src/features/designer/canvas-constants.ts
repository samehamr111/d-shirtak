/**
 * Re-exported from @d-shirtak/shared, the single source of truth for the designer canvas's
 * reference frame — the admin mockup generator/aligner and the seed script must agree with this
 * exactly, or the print-area guide box drifts off the garment (see canvas-frame.ts for the full
 * explanation of why).
 */
export { CANVAS_WIDTH, CANVAS_HEIGHT, PX_PER_CM } from "@d-shirtak/shared";
