{
  "name": "d-shirtak",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,storefront,admin -c blue,green,magenta \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/storefront\" \"npm run dev --workspace=apps/admin\"",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/storefront && npm run build --workspace=apps/admin",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "seed": "npm run seed --workspace=apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "prisma@5.22.0": true,
    "@prisma/client@5.22.0": true,
    "@prisma/engines@5.22.0": true,
    "esbuild@0.28.1": true,
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true
  }
}
